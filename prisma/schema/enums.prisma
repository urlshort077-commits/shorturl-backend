enum Role {
    USER
    ADMIN
}

enum Plan {
    FREE
    PRO
    ULTIMATE
}

enum UserStatus {
    ACTIVE
    SUSPENDED
}

enum UrlStatus {
    AVAILABLE
    RESTRICTED
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    REFUNDED
}

enum SubscriptionStatus {
    ACTIVE
    CANCELLED
    EXPIRED
}
